{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Current Module",
            "type": "python",
            "request": "launch",
            "module": "${fileDirname}.${fileBasenameNoExtension}",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}